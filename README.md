# bestrepoever

What each file is:

- The notebooks are mostly used for exploration and to guide us.
- The py files have the reusable code

How to read the files:

- To run the code, just run the main.py file